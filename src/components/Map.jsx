import React from "react";
import { exportMap } from "../assets";

const Map = () => {
  return (
    <div>
      <img src={exportMap} alt="exportMap" className="flex justify-center items-center w-screen" />
    </div>
  );
};

export default Map;
